// document.getElementById('main-form').addEventListener('click', function(el){
    // el.preventDefault();


  // проверяем, что клик был по элементу option
//   if (el.target.tagName === 'OPTION') {
    // console.log(el.target)

    // если элемент уже был выбран, то отменяем его выбор
    // if (el.target.selected) {
    //   el.target.selected = false;
    // }
//   }
// }, false);






// sel.addEventListener('click', (el)=>{el.preventDefault(); if(el.target.selected){el.target.selected=!el.target.selected}})
// VM2275:1 Uncaught ReferenceError: sel is not defined
//     at <anonymous>:1:1
// (anonymous) @ VM2275:1
// let sel = document.getElementById('id_type')
// undefined
// sel.addEventListener('click', (el)=>{el.preventDefault(); if(el.target.selected){el.target.selected=!el.target.selected}})
// undefined
